<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/theme" /> <!-- Change this color as needed -->
    <size
        android:width="120dp"
        android:height="90dp" />
    <corners android:radius="15dp" />
</shape>